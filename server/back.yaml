apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: back
  name: back
spec:
  replicas: 1
  selector:
    matchLabels:
      app: back
  template:
    metadata:
      labels:
        app: back
    spec:

      volumes:
        - name: google-cloud-key
          secret:
            secretName: backend-service-account

      containers:
        - image: gcr.io/talkiewalkie-305117/talkiewalkie-back:86d82d9f61bead709fed7e54db26d8bae73c2700
          imagePullPolicy: Always
          name: back
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: google-cloud-key
              mountPath: /var/secrets/google
          livenessProbe:
            exec:
              command: [ "/bin/grpc_health_probe", "-addr=:8080" ]
            httpGet:
              port: 8080
              path: /
            timeoutSeconds: 5
            periodSeconds: 30
            failureThreshold: 2
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: pg_password
                  name: mysecrets
            - name: POSTGRES_HOST
              value: "pgsql"
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/var/secrets/google/key.json"
            - name: POSTGRES_USER
              value: "postgres"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  key: jwt_secret
                  name: mysecrets
            - name: BUCKET_NAME
              valueFrom:
                secretKeyRef:
                  key: bucket_name
                  name: mysecrets

      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: back
spec:
  type: ClusterIP
  selector:
    app: back
  ports:
    - port: 8080

