// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/cheekybits/genny

package entities

type MessageSlice []Message
type MessageSlicePtrs []*Message

func (slice MessageSlice) UniqueBy(keyer func(Message) interface{}) MessageSlice {
	u := map[interface{}]Message{}

	for _, item := range slice {
		key := keyer(item)
		u[key] = item
	}

	out := []Message{}
	for _, item := range u {
		out = append(out, item)
	}
	return out
}

func (slice MessageSlice) FilterBy(predicate func(Message) bool) MessageSlice {
	out := []Message{}
	for _, item := range slice {
		if predicate(item) {
			out = append(out, item)
		}
	}

	return out
}

func (slice MessageSlice) Contains(t Message) bool {
	for _, item := range slice {
		if item == t {
			return true
		}
	}

	return false
}

func (slice MessageSlice) IsEmpty() bool {
	return len(slice) == 0
}

// from https://stackoverflow.com/a/36000696
func (slice MessageSlice) SameAs(other MessageSlice) bool {
	if len(slice) != len(other) {
		return false
	}
	// create a map of string -> int
	diff := make(map[Message]int, len(slice))
	for _, _x := range slice {
		// 0 value for int is 0, so just increment a counter for the string
		diff[_x]++
	}

	for _, _y := range other {
		// If the string _y is not in diff bail out early
		if _, ok := diff[_y]; !ok {
			return false
		}
		diff[_y] -= 1
		if diff[_y] == 0 {
			delete(diff, _y)
		}
	}

	return len(diff) == 0
}

//
// Slice of pointers
//

func (slice MessageSlicePtrs) UniqueBy(keyer func(*Message) interface{}) MessageSlicePtrs {
	u := map[interface{}]*Message{}

	for _, item := range slice {
		key := keyer(item)
		u[key] = item
	}

	out := []*Message{}
	for _, item := range u {
		out = append(out, item)
	}
	return out
}

func (slice MessageSlicePtrs) FilterBy(predicate func(*Message) bool) MessageSlicePtrs {
	out := []*Message{}
	for _, item := range slice {
		if predicate(item) {
			out = append(out, item)
		}
	}

	return out
}

func (slice MessageSlicePtrs) Contains(t *Message) bool {
	for _, item := range slice {
		if item == t {
			return true
		}
	}

	return false
}

func (slice MessageSlicePtrs) FilterNotNil() MessageSlicePtrs {
	return slice.FilterBy(func(t *Message) bool {
		return t != nil
	})
}

func (slice MessageSlicePtrs) IsEmpty() bool {
	return len(slice) == 0
}

// from https://stackoverflow.com/a/36000696
func (slice MessageSlicePtrs) SameAs(other MessageSlicePtrs) bool {
	if len(slice) != len(other) {
		return false
	}
	// create a map of string -> int
	diff := make(map[Message]int, len(slice))
	for _, _x := range slice {
		// 0 value for int is 0, so just increment a counter for the string
		diff[_x]++
	}

	for _, _y := range other {
		// If the string _y is not in diff bail out early
		if _, ok := diff[_y]; !ok {
			return false
		}
		diff[_y] -= 1
		if diff[_y] == 0 {
			delete(diff, _y)
		}
	}

	return len(diff) == 0
}
