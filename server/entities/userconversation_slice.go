// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/cheekybits/genny

package entities

type UserConversationSlice []UserConversation
type UserConversationSlicePtrs []*UserConversation

func (slice UserConversationSlice) UniqueBy(keyer func(UserConversation) interface{}) UserConversationSlice {
	u := map[interface{}]UserConversation{}

	for _, item := range slice {
		key := keyer(item)
		u[key] = item
	}

	out := []UserConversation{}
	for _, item := range u {
		out = append(out, item)
	}
	return out
}

func (slice UserConversationSlice) FilterBy(predicate func(UserConversation) bool) UserConversationSlice {
	out := []UserConversation{}
	for _, item := range slice {
		if predicate(item) {
			out = append(out, item)
		}
	}

	return out
}

func (slice UserConversationSlice) Contains(t UserConversation) bool {
	for _, item := range slice {
		if item == t {
			return true
		}
	}

	return false
}

func (slice UserConversationSlice) IsEmpty() bool {
	return len(slice) == 0
}

// from https://stackoverflow.com/a/36000696
func (slice UserConversationSlice) SameAs(other UserConversationSlice) bool {
	if len(slice) != len(other) {
		return false
	}
	// create a map of string -> int
	diff := make(map[UserConversation]int, len(slice))
	for _, _x := range slice {
		// 0 value for int is 0, so just increment a counter for the string
		diff[_x]++
	}

	for _, _y := range other {
		// If the string _y is not in diff bail out early
		if _, ok := diff[_y]; !ok {
			return false
		}
		diff[_y] -= 1
		if diff[_y] == 0 {
			delete(diff, _y)
		}
	}

	return len(diff) == 0
}

//
// Slice of pointers
//

func (slice UserConversationSlicePtrs) UniqueBy(keyer func(*UserConversation) interface{}) UserConversationSlicePtrs {
	u := map[interface{}]*UserConversation{}

	for _, item := range slice {
		key := keyer(item)
		u[key] = item
	}

	out := []*UserConversation{}
	for _, item := range u {
		out = append(out, item)
	}
	return out
}

func (slice UserConversationSlicePtrs) FilterBy(predicate func(*UserConversation) bool) UserConversationSlicePtrs {
	out := []*UserConversation{}
	for _, item := range slice {
		if predicate(item) {
			out = append(out, item)
		}
	}

	return out
}

func (slice UserConversationSlicePtrs) Contains(t *UserConversation) bool {
	for _, item := range slice {
		if item == t {
			return true
		}
	}

	return false
}

func (slice UserConversationSlicePtrs) FilterNotNil() UserConversationSlicePtrs {
	return slice.FilterBy(func(t *UserConversation) bool {
		return t != nil
	})
}

func (slice UserConversationSlicePtrs) IsEmpty() bool {
	return len(slice) == 0
}

// from https://stackoverflow.com/a/36000696
func (slice UserConversationSlicePtrs) SameAs(other UserConversationSlicePtrs) bool {
	if len(slice) != len(other) {
		return false
	}
	// create a map of string -> int
	diff := make(map[UserConversation]int, len(slice))
	for _, _x := range slice {
		// 0 value for int is 0, so just increment a counter for the string
		diff[_x]++
	}

	for _, _y := range other {
		// If the string _y is not in diff bail out early
		if _, ok := diff[_y]; !ok {
			return false
		}
		diff[_y] -= 1
		if diff[_y] == 0 {
			delete(diff, _y)
		}
	}

	return len(diff) == 0
}
